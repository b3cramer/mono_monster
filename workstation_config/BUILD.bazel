load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "brew_install",
    srcs = ["brew_install.sh"],
)

sh_binary(
    name = "git_automation",
    srcs = ["shell_automations.sh"],
)
